---
# enable all checks except the named ones (they are subtracted):
Checks: '*,-altera-*,-android-*,-fuchsia-*,-google-*,-zircon-*,-abseil-*,-llvm*,-objc-*,-modernize-use-trailing-return-type'

# no warning as errors
WarningsAsErrors: ''

HeaderFilterRegex: ''
FormatStyle:     none
